language: java
dist: xenial

install: gradle

before_install:
  - sudo apt-get update
  - sudo apt-get install mysql-server

script:
  - sudo mysql --user="root" --execute="CREATE SCHEMA IF NOT EXISTS noSkypeTest;"
  - sudo mysql -u root noSkypeTest <
  - gradle build && gradle bootRun


